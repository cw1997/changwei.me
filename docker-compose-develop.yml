version: '3'

services:
  homepage_dev:
    container_name: changwei-homepage-dev
    image: changwei1997/changwei.me:dev
    ports:
      - "30001:30002"
    networks:
      - nginx_bridge
networks:
  nginx_bridge:
    external: true
